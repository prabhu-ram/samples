Helps to go in sequential order. 1hw.html 2interpolate_binding.html, etc. Open them in a browser and then see the source to understand.

Some files are under tryaungular directory and launch them using cd tryangular & npm start and then view them through a browser. Installs for tryangular:
- cd tryangular
- npm install --save (for one time installation of the node_modules libraries)
then you can npm start.

